/*defs.mainBoard.addEventListener('scroll', () => {
    if(defs.mainBoard.scrollTop == 0) {
        defs.mainBoard.scrollBy({
            left: 0, 
            top: 16,
            //behavior: "smooth"
        });
    }
})*/